<%= form_for @intowel, :html => { :class => "form-horizontal intowel" } do |f| %>

  <% if @intowel.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@intowel.errors.count, "error") %> prohibited this intowel from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @intowel.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label 'cliente', :class => 'control-label col-lg-2' %>
    <div class="col-lg-4">
      <%= f.collection_select(:client_id, @clients, :id, :name, {include_blank: 'Selecione o cliente:'},:class => 'form-control') %>
    </div>
    <%=f.error_span(:client_id) %>
  </div>
  

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit 'Salvar', :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                intowels_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
